# Pi authenticates a ChatGPT Plus/Pro subscription through the Settings page's
# Authorise Codex flow; the OAuth credential is stored in the recipes database,
# so do not put ChatGPT credentials or OAuth tokens in this file.
PI_MODEL=gpt-5.4-mini
# Set to false to generate recipes without OpenAI's native web_search tool.
PI_SEARCH_ENABLED=true
# Optional local overrides for the Pi SDK worker and codex-auth helper paths.
# PI_WORKER_PATH=pi/recipe-worker.mjs
# PI_AUTH_SCRIPT_PATH=pi/codex-auth.mjs
# Agent dir for the Pi SDK's model cache; a legacy auth.json here is imported
# into the database once at startup.
# PI_CODING_AGENT_DIR=~/.pi/agent
DATABASE_URL=sqlite:/data/recipes.sqlite3
APP_BIND=0.0.0.0:3000
RUST_LOG=kindle_recipes=info,tower_http=info

# Social recipe-video importer. Docker installs these local tools by default:
# yt-dlp downloads public Facebook/Instagram media, ffmpeg extracts audio and
# frequent screenshots, faster-whisper transcribes audio locally, and the
# default PaddleOCR PP-OCRv6 reads screenshots in one batched process. The
# Whisper and PaddleOCR models are downloaded once into the persistent /data
# volume and are never sent to an AI API. Set these only for custom installs.
# MEDIA_YTDLP_PATH=yt-dlp
# MEDIA_FFMPEG_PATH=ffmpeg
# MEDIA_OCR_ENGINE=paddle
# MEDIA_OCR_SCRIPT=pi/local-media.py
# MEDIA_OCR_VERSION=PP-OCRv6
# MEDIA_OCR_MODEL_SIZE=small  # medium, small, or tiny
# MEDIA_OCR_LANG=en
# MEDIA_OCR_BATCH_SIZE=8
# MEDIA_OCR_SCORE_THRESHOLD=0.70
# MEDIA_OCR_DEVICE=cpu
# Dynamic OCR cadence: the sampler measures how fast on-screen text changes
# (ffmpeg signalstats YDIF per sample) and scans at SCAN_HZ while it churns,
# decaying to QUIET_HZ sentinels while a card holds still. Set
# MEDIA_OCR_ADAPTIVE=false for the legacy fixed-stride grid. Raise SCAN_HZ
# (e.g. 4) for fast-cut reels; thresholds are luma-change scores (0-255).
# MEDIA_OCR_ADAPTIVE=true
# MEDIA_OCR_SCAN_HZ=2
# MEDIA_OCR_QUIET_HZ=0.4
# MEDIA_OCR_YDIF_ACTIVE=6.0
# MEDIA_OCR_YDIF_QUIET=1.5
# MEDIA_TESSERACT_PATH=tesseract  # fallback: MEDIA_OCR_ENGINE=tesseract
# MEDIA_TESSERACT_LANG=eng  # use eng+ara for bilingual Tesseract fallback
# MEDIA_PYTHON=/opt/media-venv/bin/python
# MEDIA_TRANSCRIBE_SCRIPT=pi/local-media.py
# MEDIA_WHISPER_MODEL=base
# MEDIA_WHISPER_COMPUTE_TYPE=int8
# MEDIA_MODEL_CACHE=/data/media-models
# Required for video imports: raw caption/transcript/OCR are reduced to
# recipe-only facts before the final recipe-generation model sees them.
# The key is used only by the worker and is never stored in SQLite or logged.
# OPENROUTER_API_KEY=
# OPENROUTER_CLEANER_MODEL=google/gemma-4-26b-a4b-it:free
# The cleaner runs with reasoning OFF by default (it wastes ~1200 tokens of
# chain-of-thought per call on the free Gemma model for no quality gain and
# routinely hits the 180s worker timeout). Set to "on" to re-enable it.
# OPENROUTER_CLEANER_REASONING=off
# OPENROUTER_CLEANER_MAX_TOKENS=2048
# OPENROUTER_CLEANER_TEMPERATURE=0
# Optional OpenRouter metadata / local test override.
# OPENROUTER_HTTP_REFERER=http://localhost:3000
# OPENROUTER_APP_TITLE=Kindle Recipes
# OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
# Optional for public posts that require a locally configured yt-dlp cookie
# strategy; do not commit cookie files or expose them through the UI.
# MEDIA_COOKIES_FILE=/run/secrets/social-cookies.txt
